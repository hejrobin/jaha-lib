{
	"private": true,
	"name": "jaha-lib",
	"version": "0.1.4",
	"description": "JavaScript libraries to interact with JAHA-API.",
	"main": "./dist/lib.js",
	"scripts": {
		"test": "echo \"Error: no test specified\" && exit 1",
		"compile": "babel -d dist/ src/"
	},
	"author": "Robin Grass <hej@carbin.se>",
	"license": "MIT",
	"dependencies": {
		"babel-preset-es2015": "^6.18.0",
		"babel-preset-stage-0": "^6.16.0",
		"whatwg-fetch": "^1.0.0"
	}
}
